# Production requirements - CPU-only for Fly.io
fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas
numpy
scikit-learn
pydantic[email]
python-multipart
xgboost
joblib
scipy
requests==2.32.3

# Authentication (legacy - being replaced by Supabase)
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# Supabase (new)
supabase==2.10.0

# Database (legacy - being replaced by Supabase)
sqlalchemy
psycopg2-binary
python-dotenv

# Email
resend

# GLiNER2 ML dependencies - CPU only
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.35.0
safetensors>=0.4.0

# DuckDB for fast analytical queries
duckdb>=1.0.0
